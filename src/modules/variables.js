const list = document.querySelector('#todo-list');
const form = document.querySelector('form');
const todo = document.querySelector('#todo');
const clear = document.querySelector('#clear');

export {
  list, form, todo, clear,
};